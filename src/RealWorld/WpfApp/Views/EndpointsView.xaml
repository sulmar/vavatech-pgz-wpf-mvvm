<Page x:Class="WpfApp.Views.EndpointsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp.Views"
      DataContext="{Binding Source={StaticResource Locator}, Path=EndpointsViewModel}"
      xmlns:scm="clr-namespace:System.ComponentModel;assembly=WindowsBase"
      xmlns:ap="clr-namespace:WpfApp.AttachedProperties"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="EndpointsView">

    <!--<Page.Resources>
        <CollectionViewSource x:Name="cvs" x:Key="EndPointViewSource" Source="{Binding EndPointsPaged}">

            <CollectionViewSource.SortDescriptions>
                --><!--<scm:SortDescription PropertyName="Name" Direction="Descending" />-->
                <!--<scm:SortDescription PropertyName="Category" Direction="Ascending" />--><!--
                <scm:SortDescription PropertyName="IsActive" Direction="Ascending"  />
                
            </CollectionViewSource.SortDescriptions>
            
        </CollectionViewSource>
    </Page.Resources>-->

    <Grid>

        <ListBox 
                ItemsSource="{Binding EndPointsPaged}" 
                ap:SortHelper.PropertyName="Name"                
            
                ItemTemplate="{StaticResource EndpointDataTemplate}"
                ScrollViewer.HorizontalScrollBarVisibility="Disabled"                
            
                RenderTransformOrigin="0.5, 0.5"
            >

            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Opacity" Value="0" />
                    <Style.Triggers>
                        <EventTrigger RoutedEvent="Loaded">
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                     From="0" To="1"
                                                     Duration="0:0:0.5" />
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </Style.Triggers>
                </Style>
            </ListBox.ItemContainerStyle>

            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>

            <ListBox.RenderTransform>
                <RotateTransform Angle="0" />
            </ListBox.RenderTransform>

        </ListBox>

        <Button 
            Template="{StaticResource RoundedButtonTemplate}"
            HorizontalAlignment="Left" 
            BorderBrush="Red"
            Style="{StaticResource NavigationButtonStyle}"
            Command="{Binding PrevPageCommand}">Prev</Button>

        <Button 
            Background="Green"
            Template="{StaticResource RoundedButtonTemplate}"
            HorizontalAlignment="Right" 
            Style="{StaticResource NavigationButtonStyle}"
            Command="{Binding NextPageCommand}">Next</Button>
           
              
    </Grid>
</Page>
