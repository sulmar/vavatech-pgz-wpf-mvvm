<Page x:Class="WpfApp.Views.EndpointsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp.Views"
      DataContext="{Binding Source={StaticResource Locator}, Path=EndpointsViewModel}"
      xmlns:scm="clr-namespace:System.ComponentModel;assembly=WindowsBase"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="EndpointsView">

    <Page.Resources>
        <CollectionViewSource x:Key="EndPointViewSource" Source="{Binding EndPoints}">

            <CollectionViewSource.SortDescriptions>
                <!--<scm:SortDescription PropertyName="Name" Direction="Descending" />-->
                <!--<scm:SortDescription PropertyName="Category" Direction="Ascending" />-->
                <scm:SortDescription PropertyName="IsActive" Direction="Ascending" />
            </CollectionViewSource.SortDescriptions>
            
        </CollectionViewSource>
    </Page.Resources>

    <Grid>

        <ListBox 
                ItemsSource="{Binding Source={StaticResource EndPointViewSource}}" 
                ItemTemplate="{StaticResource EndpointDataTemplate}"
                ScrollViewer.HorizontalScrollBarVisibility="Disabled" >

                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>

        </ListBox>


        <Button HorizontalAlignment="Left" Width="50" Height="50" Command="{Binding PrevPageCommand}">Prev</Button>

        <Button HorizontalAlignment="Right" Width="50" Height="50" Command="{Binding NextPageCommand}">Next</Button>
           
              
    </Grid>
</Page>
