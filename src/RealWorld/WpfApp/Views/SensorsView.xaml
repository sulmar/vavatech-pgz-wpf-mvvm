<Page x:Class="WpfApp.Views.SensorsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp.Views"
      xmlns:vm="clr-namespace:ViewModels;assembly=ViewModels"
      xmlns:uc="clr-namespace:WpfApp.CustomControls"
      DataContext="{Binding SensorsViewModel, Source={StaticResource Locator}}"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="SensorsView">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>


        <ListBox ItemsSource="{Binding Sensors}" SelectedItem="{Binding SelectedSensor}" >

            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="2" />
                    <!--<StackPanel />-->
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <Label Content="{Binding Name}" Width="200" />
                        <Label Content="{Binding IpAddress}" />

                        <!--<TextBlock Text="{Binding X}" />
                        <TextBlock Text="{Binding Y}" />-->

                        <TextBlock>
                            <TextBlock.Text>
                                <MultiBinding StringFormat="{} (x:{0} y:{1})">
                                    <Binding Path="X" />
                                    <Binding Path="Y" />
                                </MultiBinding>
                            </TextBlock.Text>
                        </TextBlock>

                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>

        </ListBox>

        <StackPanel Grid.Column="1" >
            <Label>Name</Label>
            <TextBox Text="{Binding SelectedSensor.Name}" />

            <Label>Ip</Label>
            <TextBox Text="{Binding SelectedSensor.IpAddress}" />

            <Label>X</Label>
            <Slider Value="{Binding SelectedSensor.X}"
                    Minimum="0"
                    Maximum="300"
                    TickFrequency="1"
                    IsSnapToTickEnabled="True"
                    />

            <Label>Y</Label>
            <Slider Value="{Binding SelectedSensor.Y}"
                     Minimum="0"
                     Maximum="300"
                     TickFrequency="1"
                    IsSnapToTickEnabled="True"
                    />


        </StackPanel>
    </Grid>
</Page>
